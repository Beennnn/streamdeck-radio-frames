# TASKS — streamdeck-radio-frames

- ☐ Manual: create GitHub repo `Beennnn/streamdeck-radio-frames` + push (needs Beennnn's `gh`/GitHub auth) → mirrors the local repo per the ~/dev git-only rule
- ☐ Optional: submit to Elgato Maker Console (maker.elgato.com) — needs Beennnn's Elgato login + Maker Agreement + final human Submit click. Run `bin/maker-media.sh`-style `sdicons maker-media` first for the upload assets.
- ☐ APRÈS PUBLICATION Maker Console : mettre à jour la description du produit sur le Marketplace pour qu'elle cite le plugin Trevligaspel MIDI (se.trevligaspel.midi) → la description avait été auto-remplie depuis l'ancien manifest à l'upload ; le manifest du repo est corrigé (commit "cite Trevligaspel MIDI") mais le listing Marketplace garde l'ancienne version tant qu'on ne l'édite pas dans le console (Products → le pack → Edit description).
- 🤔 Consider: a two-tone corner-notch variant (one colour left, another right) to mark two exclusive groups on one panel, like the funk cyan/amber split — only if a single-colour-per-group isn't enough in practice.
