# TASKS — streamdeck-radio-frames

- ☐ Manual: create GitHub repo `Beennnn/streamdeck-radio-frames` + push (needs Benoît's `gh`/GitHub auth) → mirrors the local repo per the ~/dev git-only rule
- ☐ Optional: submit to Elgato Maker Console (maker.elgato.com) — needs Benoît's Elgato login + Maker Agreement + final human Submit click. Run `bin/maker-media.sh`-style `sdicons maker-media` first for the upload assets.
- 🤔 Consider: a two-tone corner-notch variant (one colour left, another right) to mark two exclusive groups on one panel, like the funk cyan/amber split — only if a single-colour-per-group isn't enough in practice.
